server: 
  port: 9001
spring: 
  application: 
    name: users-management-service
  data:
      mongodb.host: localhost
      mongodb.port: 27017
      mongodb.uri: mongodb://localhost/file-storageTT
      mongo.repositories.enabled: true 
multipart.maxFileSize: 100MB
multipart.maxRequestSize: 100MB

security:
  basic:
    enabled: false
  user:
    name: user
    password: user
